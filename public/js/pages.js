const stampbookBtn = document.getElementById('stamps');
const mapPageBtn = document.getElementById('map');

const renderStampsPage = () => window.location = '/stampbook';
const renderMapPage = () => window.location = '/';